# docker-compose environment. Copy to .env and fill in.

# Postgres
DB_PASSWORD=bitirme

# Python worker runtime
FAST_MODE=True
B_BOOT=150
DRIFT_EPS=0.20
USE_GPU_XGB=False

# Rust controller runtime
MAX_PARALLEL_JOBS=8
RUST_LOG=info,sqlx=warn
FORECAST_HTTP_TIMEOUT_SECS=1800
